# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*
.pnpm-debug.log*

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Coverage directory used by tools like istanbul
coverage
*.lcov
.nyc_output

# Dependency directories
node_modules/
jspm_packages/

# TypeScript cache
*.tsbuildinfo

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Optional stylelint cache
.stylelintcache

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity
.yarn/install-state.gz
.yarn-build-state.yml

# dotenv environment variable files - CRITICAL FOR SECURITY
.env
.env.*
!.env.example
.env.local
.env.development.local
.env.test.local
.env.production.local

# parcel-bundler cache (https://parceljs.org/)
.cache
.parcel-cache

# Next.js build output - SPECIFIC TO OUR STACK
.next
out
.next/static/chunks/
.next/static/development/
.next/static/webpack/

# Expo/React Native - SPECIFIC TO OUR MOBILE APP
mobile/.expo/
mobile/dist/
mobile/web-build/
mobile/__generated__/
mobile/.nx/
mobile/.expo-shared/

# Supabase - SPECIFIC TO OUR BACKEND
supabase/.temp/
supabase/functions/*/dist/
supabase/.env
supabase/.env.local

# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE files
.vscode/
.idea/
*.swp
*.swo
*~
.nova/

# Lockfiles (we want to commit these, so we ignore the ignore)
!package-lock.json
!yarn.lock
!pnpm-lock.yaml

# ===== REMOVED UNNECESSARY ITEMS =====
# Removed: .grunt (we don't use Grunt)
# Removed: bower_components (we don't use Bower)
# Removed: .lock-wscript (legacy)
# Removed: build/Release (native addons - we don't have any)
# Removed: snowpack web_modules (we don't use Snowpack)
# Removed: .node_repl_history (irrelevant)
# Removed: Nuxt.js, Gatsby, Vuepress, Sveltekit, Vite, VitePress, Docusaurus (we use Next.js)
# Removed: Serverless, FuseBox, DynamoDB, Firebase, TernJS (not in our stack)
# Removed: .vscode-test (we don't test VSCode extensions)
# Removed: excessive Yarn v3 rules (we use standard Yarn/npm)
